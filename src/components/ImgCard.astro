---
export interface Props {
  body: string;
  imgsrc: string;
  alt: string;
}

const { imgsrc, alt, body } = Astro.props;
---

<div class="card shadow-sm">
  <img src={imgsrc} alt={alt} />
  <div class="card-body">
    <p class="card-text">{body}</p>
  </div>
</div>
<style>
  .card {
    border: none;
  }

  img {
    border-top-left-radius: 0.375rem;
    border-top-right-radius: 0.375rem;
  }
</style>
