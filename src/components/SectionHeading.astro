---
const { heading, description } = Astro.props;
---

<h3 class="display-6">{heading}</h3>
<p class="lead">{description}</p>
<style lang="scss">
  h3 {
    border-bottom: 2px solid var(--sim-red);
    padding-bottom: 0.5rem;
    + p {
      margin-bottom: 4rem;
    }
  }
</style>
